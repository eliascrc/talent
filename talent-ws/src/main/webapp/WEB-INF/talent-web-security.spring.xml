<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:security="http://www.springframework.org/schema/security"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/security
		http://www.springframework.org/schema/security/spring-security.xsd">

    <security:authentication-manager alias="authenticationManager">
        <security:authentication-provider ref="technicalResourceAuthenticationProvider"/>
    </security:authentication-manager>

    <!-- Note: auto-config was set to true because since there is no entry point it would give an error when running tomcat
         It will be changed with the Spring Security Rules ticket.-->
    <security:http use-expressions="false" pattern="/**/*" auto-config="true"/>

</beans>